#include<stdio.h>
#include<string.h> 
const int maxn=1010;
int main(){
	char a[maxn];
	int ans[maxn];
	int b;
	scanf("%s%d",a,&b);
	int len=strlen(a);
	int num=0,r=0;
	for(int i=0;i<len;i++){
		ans[num]=(r*10+(a[i]-'0'))/b;
		r=(r*10+(a[i]-'0'))%b;
		num++;
	}
	int k=0;
	for(int i=0;i<num;i++){
		if(ans[i]!=0){
			k=i;
			break;
		}
	}
	for(int i=k;i<num;i++){
		printf("%d",ans[i]);
	}
	printf(" %d",r);
	return 0;
}

































































































































































































































































































































































































